"""评测 —— 把感知/重定向的输出拿去和真值比，给论文里那些数字。

和流水线其他包不同：这里的模块**不参与生产**，只用来产出可复核的结论。所以它们的
依赖也只有 numpy —— 感知那部分的中间结果已经冻进 `evidence/` 了（见各模块说明），
评测本身秒级可跑，不需要 GPU、不需要外部数据集。

- :mod:`~web2robot.eval.depth_benchmark` —— HO-3D 手腕定位：WiLoR+MoGe vs HaWoR
  的深度/平面误差与深度跟随。这是"单目深度是硬瓶颈、HaWoR 是解法"这个结论的证据。
"""
