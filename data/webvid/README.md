# data/webvid/ —— 2026-07 手工找的网络片段

第①步（视频质检/路由）和第③步（感知前端）当时的试验素材，7 段共 55 MB。
2026-08-10 从 `external/HaWoR/webvid/` 挪过来的 —— 原来它躺在第三方 checkout 里，
`git clean` 一下就没了，而这些是**手工找的、没有下载脚本可以重来**。

| 文件 | 时长 | 分辨率 | 当时拿来干什么 |
|---|---|---|---|
| `webclip.mp4` | 47.3s | 640×360 | 最早的探路片段 |
| `cand2.mp4` | 41.6s | 640×360 | 候选片段，出过 `cand_grid.jpg` |
| `v1.mp4` `v2.mp4` `v3.mp4` | 40 / 24 / 78s | 640×360 | 三段对比，`v2_timeline.jpg` 是它的时间线 |
| `e1.mp4` | 411.5s | 640×360 | 长片，用来看切镜和长段落的坏帧 |
| `demo_src.mkv` | 280.7s | 640×480 | HaWoR demo 那段的原片（原名 `demo/src.mkv`） |

`MANIFEST.md5` 是搬家当时的校验和，7/7 逐字节核对过。想确认没被动过：

```bash
cd data/webvid/raw && md5sum -c MANIFEST.md5
```

## 派生物不在这

抽帧、contact sheet、demo 渲染那 429 个文件全在
`outputs/archive/webvid_2026-07/`（13 MB）—— 它们都能从上面这 7 段重新算出来，
所以归 `outputs/`。只有算不回来的原片归 `data/`，这条线就是 `data/` 和 `outputs/` 的分界。

## 这批素材的局限

都是 640×360 的小图，而且是手工挑的（挑的时候就偏向"手清楚、镜头稳"），
所以**不能拿它当质检模块的评测集** —— 选择偏差正好抵消了质检要检的东西。
真评测集得是没挑过的一批。
